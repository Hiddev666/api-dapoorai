generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "postgresql"
    url = env("DATABASE_URL")
}

model Test {
    uuid String @id @default(uuid())
    name String
    @@map("test")
}
